# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
build
*.local

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# TypeScript
*.tsbuildinfo

# Phase 14.14s — `tsc --noEmit > tsc.out` diagnostic dumps don't belong in git
tsc.out

# Build emission from `tsc -b` (vite.config.ts compiled outputs)
vite.config.js
vite.config.d.ts

# Testing
coverage
.nyc_output

# Supabase local dev (if used later)
supabase/.branches
supabase/.temp

# Claude Code working directory
.claude/

# Ignore corrupt ghost files from bad shell copy commands
supabase/migrations/*&&*
